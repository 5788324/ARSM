# ARSM 项目交接文档

日期：2026-06-21 | 最新确认提交：`c6604a1` | 状态：Phase 9 已验收通过，进入 Phase 10 规划

---

## 当前结论

ARSM 当前已经不是“只有代码完成”的状态，而是已经完成到 **Phase 9 并经过真实页面复核** 的版本。

本轮关键结论：

- `asmr.one` 采集链路可用
- 作品库搜索、高级筛选、URL 同步可用
- `/continue` 统计页已通过真实验收
- `PlayLog` 缺表问题已通过 `db push` 修复路径确认
- “最常播放”次数显示问题已在 `c6604a1` 用原始 SQL 修复，并在真实页面看到 `2 / 1`

---

## 当前已完成能力

### 采集与导入

- `asmr.one` provider：`inspect -> download -> import`
- 下载任务列表、文件级进度、错误状态
- 本地目录导入
- 重复检测与 review
- DLsite provider：当前仅元数据级 `inspect`

### 元数据

- 自动补全 asmr.one 元数据
- 社团 / 声优 / 标签关联
- 管理员编辑标题、原标题、发售日
- 管理员重新抓取 asmr.one
- 个人评分独立存储

### 播放与字幕

- 全局播放器
- 队列、循环、倍速、续播
- 曲目目录分组
- `.lrc / .vtt / .srt` 时间轴字幕
- 字幕跟播高亮、自动滚动、点击跳转
- `.txt / .pdf` 文本字幕阅读

### 浏览与用户数据

- 首页：最近添加、最近采集、最近播放、快捷入口
- 作品库：搜索、排序、分页、URL 同步
- 高级筛选：排除词、时长、社团、声优、标签、带字幕
- 关系页：社团 / 声优 / 标签
- 收藏
- 继续收听页
- 播放次数、收听作品数、总收听时长、最常播放

### 运行与部署

- `启动ARSM.bat` / `停止ARSM.bat`
- Docker 基础支持
- 深色模式

---

## 本轮真实验收结果

### 已真实验证

- `/works`
  - 高级筛选面板可展开
  - 社团 / 声优 / 标签输入框已真实渲染
  - URL 会同步 `circleName / vaName / tagName`
- `/continue`
  - 页面可正常打开
  - `play_logs` 缺表问题可通过 `prisma db push` 修复
  - “最常播放”在真实页面显示为：
    - `RJ01538000 -> 2`
    - `RJ01564120 -> 1`

### 验收备注

- 真实浏览器验收最终是通过 `http://127.0.0.1:3002` 的生产服务完成的
- 这次验证过程中，向本地数据库写入了少量 `PlayLog` 测试数据，用于确认“最常播放”榜单显示

---

## 当前已知限制

- DLsite 仍然是 `inspect-only`
- 字幕还不支持翻译、双语对照、AI 自动生成
- 统计页目前还是基础版，不是完整“个人中心”
- Windows 启动器可用，但还不算真正意义上的桌面程序壳

---

## Phase 10 建议目标

优先做“产品收口”而不是再撒功能点。

### P1

- 统计与播放链路回归验收固化
- 采集、播放、统计三条主链的真实验收模板化
- 启动器与运行状态可视化

### P2

- 采集页产品化：更清楚的状态、结果、错误、元数据预览
- 统计页产品化：最近完成、最常播放、来源分布、收听趋势
- 搜索体验继续打磨

### P3

- 第二真实下载来源评估
- 字幕翻译 / 双语 / AI 方案设计
- PWA / Android / 桌面壳路线评估

---

## 建议下一步

不要再回头补 Phase 9。

直接进入：

1. `docs/PHASE10_ONEPASS_TASKLIST_ZH.md`
2. `ARSM_NEXT_PHASE.md`
3. `WORKLIST.md`

按一次性交付的方式让 DeepSeek 做 Phase 10，再由 Codex 做集中审查。
